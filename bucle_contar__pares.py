#!/usr/bin/env python
# -*- coding: utf-8 -*-

limite = input("Introduzca limite:")
for contador in range (0,limite+1):
	
	if contador %2 == 0:
		print "",contador

def my_range ():
	if contador == limite:
		salir = True
	contador = contador + 1
